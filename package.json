{
  "name": "morpho-store",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/web"],
  "scripts": {
    "dev": "npm run dev --workspace=@morpho/web",
    "build": "npm run build --workspace=@morpho/web",
    "lint": "npm run lint --workspace=@morpho/web",
    "typecheck": "npm run typecheck --workspace=@morpho/web"
  },
  "engines": { "node": ">=22" }
}
