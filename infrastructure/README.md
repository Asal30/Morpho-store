# Infrastructure

Local orchestration currently lives in the root `docker-compose.yml`. Production orchestration will be designed separately; Kubernetes configuration intentionally does not belong to this foundation phase.
